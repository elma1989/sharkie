import { TextButton } from "../abstract/button-text.js";

/** A Button to oppen impressum overlay. */
export class ImpressumButton extends TextButton {
    constructor() {
        super('btn-inprint');
    }
}
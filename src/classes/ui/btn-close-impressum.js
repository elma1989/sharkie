import { Button } from "../abstract/button.js";

/** A button for close impressum overlay. */
export class ImpressumCloseButton extends Button {
    constructor() {
        super('btn-close-inprint');
    }
}